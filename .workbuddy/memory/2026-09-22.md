# 2026-09-22

## 代码分析：Blinko 本地研发与测试链路

任务：分析在本地如何研发，以及研发/测试修改后的效果验证方式。

### 关键结论（源码级）

1. **单端口一体化架构，非前后端分离**
   - `server/index.ts:316` 仅监听 `PORT = 1111`，`ViteExpress.bind(app, server)` 把 Vite dev server 作为 Express 中间件挂载。
   - `app/package.json` 的 `dev` 脚本为 `cd ../server && bun run dev` — 前端没有独立 dev server。
   - 因此前后端同源同端口，无 CORS / proxy 配置需要维护。

2. **开发模式分支**：`server/index.ts:98-115`
   - `NODE_ENV === 'production'` → root = `../server`，outDir = `public`
   - 否则 → root = `../app`，加载 `app/vite.config.ts`

3. **Tauri 只是套壳**：`app/src-tauri/tauri.conf.json`
   - `beforeDevCommand: bun run dev`、`devUrl: http://localhost:1111`
   - 桌面端加载的是同一个 1111 服务，前端改动同样吃 HMR。

4. **测试框架 = bun:test**（无 jest/vitest 配置），测试位于 `server/__tests__/{unit,integration,e2e}`。

### 发现的问题（待修，尚未改动代码）

- 🔴 **`bun run test` 是空操作**：`turbo.json` 定义了 `test` 任务，但 `app`/`server`/`shared` 三个 package.json 均无 `test` 脚本，turbo 会直接跳过。实际必须 `cd server && bun test`。
- 🟡 **NODE_ENV 未设置会误启用 PWA 插件**：`app/vite.config.ts` 中 `isDev = process.env.NODE_ENV === 'development'`，未设置时 `!isDev` 为 true，绕过了「开发态禁用 PWA」的意图。建议 dev 显式传 `NODE_ENV=development`。
- 🟡 **`.env.tmpl` 数据库端口 5435 与 `docker-compose.yml` 的 5432 不一致**，易配错。
- 🟢 `dotenv turbo run dev` 前缀冗余：根依赖只有 `dotenv` 包（无 dotenv-cli CLI），而 `server` 的 dev 脚本已自带 bun 的 `--env-file ../.env`。
- 🟢 数据库强制 PostgreSQL（`prisma/schema.prisma` provider = "postgres"）；依赖中的 libsql/sqlite3 服务于 RAG 向量存储，不是主库。

### 当前环境状态（实测）

- `bun` 未安装（`which bun` → not found）
- 根 / app / server 均无 `node_modules`
- 无 `.env` 文件
- 即：仓库尚未初始化，任何 dev / test 命令当前都无法运行。

---

## 实操：环境从 0 到跑通（已完成 + 踩坑记录）

### 已落地的环境

- ✅ Bun 1.2.8 → `/Users/baihe/.bun/bin/bun`（npmmirror 二进制，GitHub 被墙 502）
- ✅ PostgreSQL 16 → `brew install postgresql@16`，启动用 `pg_ctl -D /opt/homebrew/var/postgresql@16 start`（**brew services 因 launchctl 权限失败，必须用 pg_ctl**）
- ✅ 数据库 `blinko` 已创建（用户为 mac 当前用户 `baihe`，无密码）
- ✅ 根 `node_modules` + `.prisma/client` 已生成
- ✅ `.env` 已写入，DATABASE_URL=`postgresql://baihe@localhost:5432/blinko`

### 网络避坑（关键！）

| 资源 | 可用源 | 不可用源 |
|---|---|---|
| Bun 二进制 | `registry.npmmirror.com/-/binary/bun` | `github.com`（502） |
| npm 包 | `registry.npmjs.org` | `registry.npmmirror.com`（tarball 302 后 integrity 不匹配，bun install 报 IntegrityCheckFailed） |
| Prisma engine | `PRISMA_ENGINES_MIRROR=https://registry.npmmirror.com/-/binary/prisma` | - |

### 启动命令（每次开机/新终端）

```bash
export PATH="$HOME/.bun/bin:/opt/homebrew/opt/postgresql@16/bin:$PATH"
pg_ctl -D /opt/homebrew/var/postgresql@16 start   # 若已启动会报错，可忽略
cd /Users/baihe/Code/blinko
bun run dev:backend
```

### 已落地的工程优化（今天一起改了）

- `.env.tmpl` 删除，拆分为 `.env.local.example`（本机开发）和 `.env.docker.example`（Docker 部署）
- `.env.README.md` 写完整使用说明
- `.gitignore` 改写：`.env.*` 通用忽略 + 三个白名单豁免
- 根 `package.json` 移除 `dotenv` 前缀（命令不存在，已修；server 的 dev 脚本自带 `bun --env-file`）
- 首次启动必须先 `bun run prisma:migrate:deploy` 建表，否则会报 `The table "public.accounts" does not exist`
- 第二次安装时（之前 integrity 失败后部分包残留），`react-syntax-highlighter` 装成空目录、`d3` 完全缺失，导致 vite 报 `Could not resolve "d3"`、`Failed to resolve import "react-syntax-highlighter"`。修复：删 node_modules 重装（2434 个包 8.4 秒，官方源）。**结论：bun install 失败后必须 rm -rf node_modules 重装，不能 retry。**
- **Demo 文件 404**：原 `prisma/seed.ts` 只 `mkdir(".blinko/files")` 不复制 `prisma/seedfiles/` 下的 demo 资源，导致 attachments 表里有 6 条 demo 元数据但磁盘为空，浏览器 404。**已修 seed.ts**：新增 seedfiles 拷贝逻辑，下次新环境跑 `bun run seed` 即可就位。当前直接 `cp prisma/seedfiles/* server/.blinko/files/` 解决。
- **AI 扩写显示 "Unknown error"**：根因是前端 `app/src/store/aiStore.tsx:310` 用 `item.error?.name`（错误类名）而不是 `item.error?.message`（真实报错）来显示，AI SDK provider 报 "Incorrect API key" / "model not found" / "rate limit" 全部被吞。已修：换成 `message ?? name ?? 'ai error'`，并加 `console.error` 留排查线索；catch 兜底也加 `error.data.message` 兼容 tRPC。同时后端 `server/routerTrpc/ai.ts:154` 的 `writing` procedure 加 try 包住 agent 初始化阶段，避免配置错误（如未配置 AI provider）传裸 Error。

打开 http://localhost:1111 即可。

### 改代码生效规则

- `app/src/**` → Vite HMR，浏览器秒变
- `server/**.ts` → bun --watch 自动重启，等 1-2 秒
- `prisma/schema.prisma` → 必须 `bun run prisma:migrate:dev` + `bun run prisma:generate` 后重启
- `shared/**` → 后端需手动重启

## 阿里云 OSS 接入排查（23 日凌晨，追加）
- 用户已配置 OSS（bucket: baispace-doc, 北京区域），报错 ERR_INVALID_URL → endpoint 缺 https:// 前缀
- 修复1：files.ts getS3Client 自动补 https:// 前缀 + 存量 DB 值已修正
- 修复2：OSS 不支持 path-style（SecondLevelDomainForbidden）→ forcePathStyle 按端点自动判断（aliyuncs.com → false 虚拟主机式）
- 验证：上传 200（/api/s3file/dev/xxx）、302 预签名跳转正常
- 遗留：Bucket 开了 Referer 防盗链且未允许空 Referer → 预签名 URL 被 AccessDenied（0003-00000501），需用户在 OSS 控制台改防盗链设置
- 注意：attachment.ts 文件夹删除修复疑似被 git 回退（代码已是旧版），待确认是否重新应用
- 教训：用户自跑的 dev server 占用 1111 端口时，后台任务的日志看不到请求；直接用 SDK 脚本绕过验证更快

## CDN 加速域名支持（23 日凌晨，追加）
- 新增 config key `s3CdnDomain`：shared/lib/types.ts（ZConfigKey + ZConfigSchema）
- s3file.ts：配置了 CDN 域名时，302 跳转目标改为 `https://<cdn>/<key>`（buildCdnUrl），否则走 OSS 预签名
- files.ts getS3Client 缓存 key 加入 s3CdnDomain（否则改配置后最长 24h 才生效）
- StorageSetting.tsx 增加 CDN 域名输入框；locales zh/en/zh-TW 增加 cdn-domain 键
- 已端到端验证：填 cdn.example.com → 跳转 https://cdn.example.com/dev/xxx；清空 → 回退预签名
- 前提：CDN 需配置 OSS 私有 Bucket 回源授权或 bucket 公共读；OSS 防盗链白名单需加 CDN/站点域名

## 正文写入 CDN 全链路地址（23 日凌晨，追加）
- 用户决定：粘贴图片上传后正文直接写 CDN 完整 URL（不走相对路径+token 注入方案）
- editorStore.tsx 新增 toContentFileUrl：配置 s3CdnDomain 时 /api/s3file/* → https://<cdn>/<key>；未配置回退相对路径
- 插入用 insertMD（insertValue 失焦失效，用户已两次撤销但该 bug 客观存在）
- 删除孤儿文件 app/src/lib/fileAuth.ts（ImageWrapper/useEditor 已被用户回退，无引用）
- 注意：CDN 直链绕过 Blinko 鉴权（私有桶需 CDN 私有回源授权）；笔记内容与 CDN 域名耦合

## CDN 直链 403 定位（23 日 01:00，追加）
- static001.baispace.cn 已正确 CNAME 到阿里云 CDN（kunlunpi.com），x-oss-cdn-auth: success（私有桶回源授权 OK）
- 403 根因仍是 OSS bucket referer policy（EC 0003-00000501）：CDN 会透传浏览器页面的 Referer，白名单未包含站点域名/空 Referer
- 用户需在 OSS 控制台 → baispace-doc → 数据安全 → 防盗链：加白名单 + 允许空 Referer，或关闭防盗链
- 上传链路本身正常（attachments id 31 记录存在）

## CDN 403 第二阶段定位（23 日 01:10，追加）
- 用户已修防盗链（0003-00000501 消失），现 EC 0003-00000001 "bucket acl"，且 x-oss-cdn-auth: success
- SDK 直接签名读 OSS 对象 OK（32880 字节）→ 对象与凭证没问题，纯 CDN 私有桶回源授权问题
- 用户已在 Blinko 设置填 s3CdnDomain=static001.baispace.cn（预签名 302 已指向 CDN）
- 待用户处理：CDN 控制台确认该加速域名的源站类型为"OSS 域名"并开启 OSS 私有 Bucket 回源 + 重新授权
- 顺带发现：上传未设 Content-Type（application/octet-stream），后续可优化
