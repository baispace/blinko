# 2026-09-23

## OSS 上传命名优化 + 正文图片备注置空
- files.ts 新增 generateStorageFileName：yyyymmdd-5位随机数字（如 20260923-98265.png），仅用于 S3/OSS 键（uploadFile + uploadFileStream 两条路径）；本地存储保持原命名
- getContentType 工具：PutObject 按 type/扩展名设置 Content-Type（修复 application/octet-stream 问题）；CDN 直链现在返回 image/png 且可 200 访问
- 附件展示名（attachments.name / 返回的 fileName）保留用户原始文件名
- editorStore.tsx 图片插入 `![]()` 备注置空；AttachmentRender/icons.tsx 的 InsertConextButton 同步改为 `![]()` + toContentFileUrl
- 端到端验证通过；用户确认 CDN（static001.baispace.cn）已可访问（私有桶回源授权修好后）

## AI 自动打标签污染问题（01:40）
- 现象：用户笔记被追加模型 <think> 分析文字 + #Welcome demo 标签
- 根因：AI 后处理（aiServer/index.ts tags 模式）把 result.text 原样 split(',') 追加进笔记；用户模型是推理型（输出 <think>）；#Welcome 来自 seed.ts demo 数据污染"现有标签列表"
- 修复：aiModelFactory.ts 新增 extractValidTags（剥离 <think> 块、仅从末段提取 #tag、去重限 5），aiServer/index.ts 与 routerTrpc/ai.ts autoTag 两条路径均改用
- 待用户确认：清理被污染笔记（id 10/12/13/14 内的 think 块/Welcome 标签/旧 JWT token）与 demo Welcome 笔记

## 污染数据清理（01:45，用户确认后执行）
- 清理范围：think 块 + AI 追加的 #Welcome 标签（用户未选 JWT 清理和 demo 笔记删除）
- 受影响笔记 id：9,10,11,12,13,14,16,18（UPDATE 8 条）
- 备份：/tmp/notes-backup-20260923.tsv（7条）+ /tmp/notes-backup-20260923-id11.csv
- 验证：用户笔记中 <think> 与 #Welcome 均为 0；demo 笔记 id 2/3/4/5 未动

## 标签提示词重写（01:50）
- 个人博客场景：最多 3 个标签、精准优先、优先复用现有标签、完整层级路径、语言与内容一致、只输出标签本身
- 两处同步：aiModelFactory.ts TagAgent 默认提示词 + DB config aiTagsPrompt 存量值（自定义提示词优先级高于默认值，必须都改）
- extractValidTags 默认 max 5 → 3（代码级硬限制）
- 注意：aiTagsPrompt 在设置 UI 有自定义值时会覆盖代码默认，改代码默认值时需同步改存量配置

## axios 超时 uncaughtException 排查（01:55）
- 报错：AxiosError "First argument must be an Error object"（handleRequestTimeout，follow-redirects socket emit）
- 根因：Bun 1.2.8 运行时与 axios 1.x 兼容问题——Error.call(this) 在 Bun 下不完全生效，出站请求超时时 Error.captureStackTrace 抛错；已有 uncaughtException handler 挡住，服务不挂，只是刷屏
- 修复：server/index.ts handler 增加对该特定 message 的识别，仅打一行 [axios-timeout] 警告；其余异常照常完整输出
- 根治选项：升级 Bun（现 1.2.8 → 最新），待用户决定
- 出站超时来源候选：recommandJob(10s)/plugin 下载(30s)/proxy.ts(30s)/public.ts(20s)
