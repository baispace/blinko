# 环境配置说明

> 本文件说明 Blinko 项目在不同环境下的配置方式，以及对应的配置文件。

## 一、环境分层

Blinko 严格区分**本地开发**与**生产部署**两套配置，二者的环境变量来源完全不同：

| 环境 | 配置文件 | 加载机制 | 隔离策略 |
|---|---|---|---|
| 本地开发 | `.env`（被 `.gitignore` 忽略） | `bun --env-file ../.env`（写在 `server/package.json` 的 `dev` 脚本中） | 永不进 git |
| Docker 生产 | `docker-compose.prod.yml` 的 `environment:` 字段 | Docker 容器启动时注入 | 与镜像一起分发，但不进 git |
| Kubernetes/Helm 生产 | `helm/*.yaml` 的 `env:` 或 `secretRef:` | K8s 注入 | 集群内 Secret 管理 |

**三种环境互不串扰**：本地 `.env` 与生产容器/Helm 的配置在物理上完全分离。

## 二、本地开发（macOS/Linux 本机直跑）

### 1. 准备 PostgreSQL

```bash
brew install postgresql@16
pg_ctl -D /opt/homebrew/var/postgresql@16 start
createdb blinko
```

### 2. 创建本地 .env

```bash
cp .env.local.example .env
```

按需调整 `DATABASE_URL` 中的用户名（mac 用户名为 `whoami`）。

### 3. 安装依赖并启动

```bash
bun install
bun run prisma:migrate:deploy    # 首次启动必须：建表（31 个迁移）
bun run dev:backend
```

打开 http://localhost:1111。

### 4. 关键变量说明

| 变量 | 说明 |
|---|---|
| `NODE_ENV` | **必须设 `development`**，否则 vite.config.ts 会误启用 PWA 插件（详见历史 issue） |
| `DATABASE_URL` | 本机 PostgreSQL 连接串，端口 `5432`，无密码（走系统信任认证） |
| `NEXTAUTH_URL` / `NEXT_PUBLIC_BASE_URL` | 本地访问地址，必须与实际启动端口一致（默认 1111） |

> **不需要配置** `NEXTAUTH_SECRET` 和 `JWT_SECRET`：代码中若读取到占位值或为空，会自动生成并存入数据库（见 `server/lib/helper.ts:137`）。

## 三、Docker 生产部署

### 1. 创建 .env（容器外）

```bash
cp .env.docker.example .env
```

修改以下关键项：
- `DATABASE_URL`：你的生产数据库地址（端口 `5435` 是 compose 内端口映射）
- `NEXTAUTH_URL` / `NEXT_PUBLIC_BASE_URL`：你的真实域名
- `NEXTAUTH_SECRET`：用 `openssl rand -base64 32` 生成

### 2. 启动

```bash
docker compose -f docker-compose.prod.yml --env-file .env up -d
```

或者直接把变量写进 `docker-compose.prod.yml` 的 `environment:` 字段里（推荐，配置更显式）。

### 3. 验证

```bash
curl http://localhost:1111/health
# 返回 {"status":"ok"}
```

## 四、Helm/Kubernetes 生产部署

参考 `helm/` 目录下的 Chart：

- 数据库、密钥走 Secret
- `NEXTAUTH_URL` 等走 ConfigMap
- 镜像用 `blinkospace/blinko:latest` 或自建 tag

详见 `helm/` 下各文件。

## 五、改代码后如何看到效果

| 改动位置 | 是否自动生效 | 等待时间 | 操作 |
|---|---|---|---|
| `app/src/**` 前端 | ✅ Vite HMR | < 1 秒 | 无需刷新 |
| `server/**.ts` 后端 | ✅ bun --watch | 1–2 秒 | 无需操作 |
| `shared/**` | ⚠️ 前端 HMR / 后端需重启 | — | 后端 Ctrl+C 后重启 |
| `prisma/schema.prisma` | ❌ | — | 跑 `bun run prisma:migrate:dev` + `bun run prisma:generate` 后重启 |
| `app/vite.config.ts` | ❌ | — | 重启 dev 进程 |

## 六、测试

```bash
cd /Users/baihe/Code/blinko
bun test server/__tests__/unit          # 纯函数，无依赖
bun test server/__tests__/integration   # mock prisma，无需真库
bun test server/__tests__/e2e           # 需 1111 服务运行中
```

## 七、避坑要点

1. **`bun install` 必须用官方源**：`https://registry.npmjs.org`。**不要用** `registry.npmmirror.com`，其 tarball 经 302 重定向后 sha256 与 `bun.lock` 不一致，会大批报 `IntegrityCheckFailed`。
2. **Bun 二进制从 npmmirror 下载**：因 GitHub Releases 被墙，直接装失败，改用 `https://registry.npmmirror.com/-/binary/bun/bun-v1.2.8/bun-darwin-aarch64.zip`。
3. **`brew services start postgresql@16` 可能失败**（launchctl 权限），用 `pg_ctl -D /opt/homebrew/var/postgresql@16 start` 代替。
4. **bun install 失败后必须 `rm -rf node_modules` 再装**：失败状态下包会残留空目录（如 `react-syntax-highlighter` 没 `dist/`），或完全缺失（如 `d3`），导致 vite 报 `Could not resolve`。retry 不会修复，必须清掉重装。
5. **历史踩坑**：原 `.env.tmpl` 内容是 Docker 编排用的（端口 5435、密码 mysecretpassword），新同事直接拷贝会跑不通本机。本文档已将该模板拆分为 `.env.local.example`（本机开发）和 `.env.docker.example`（容器部署），避免歧义。

## 八、文件清单

| 文件 | 进 git？ | 用途 |
|---|---|---|
| `.env` | ❌ | 当前本地实跑配置（已 .gitignore） |
| `.env.local.example` | ✅ | 本地开发模板（按场景拆分后新增） |
| `.env.docker.example` | ✅ | Docker 部署模板（按场景拆分后新增） |
| `.env.README.md` | ✅ | 本说明文档 |
| ~~`.env.tmpl`~~ | — | 已删除，逻辑已被上两个 example 替代 |
