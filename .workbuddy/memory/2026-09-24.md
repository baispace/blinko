
## 编辑器右上角浮动全屏按钮（2026-09-24 凌晨）
- 用户反馈：底部工具栏最右侧的「切换全屏」按钮位置不合理，希望上移到正文区域右上角
- 改动：Common/Editor/index.tsx
  - 从 renderRightToolbar() 中移除 FullScreenButton（底部已只剩 ViewMode + Send）
  - 在 vditor 容器外包一层 `<div class="relative ...">`，右上角绝对定位按钮
  - 仅 PC 且非 showTopToolbar 时渲染；进入全屏后相对定位的 wrapper 用 flex-1 min-h-0 保证继续显示
  - 样式：bg-background/70 backdrop-blur-sm 圆角 + 边框 + 阴影，默认 opacity-0，hover/focus-within 时显示（不打扰写作）
- Vite HMR 已自动应用，curl 校验编译产物包含新结构

## 编辑器全屏按钮：图标与配色升级（2026-09-24 00:12）
- 用户反馈："右上角没看到"，发现上一版 opacity-0 默认隐藏导致截图看不到→改为常显后可见
- 进一步反馈："图标一起改下" → 替换为 HEO 紫主色系更协调的图标
- 图标变更：basil:expand-outline / radix-icons:exit-full-screen → material-symbols:open-in-full-rounded / material-symbols:close-fullscreen-rounded（圆润厚实，更醒目）
- 配色：浮动容器从"半透明白底+灰图标"改为双色态——常态：白底+灰图标，hover 时图标转 HEO 紫+边框变紫；进入全屏后：填充 HEO 紫底+白图标，强提示

## 闪念视图设置弹层（BlinkoHomeSettingsPop，2026-09-24 00:30）
- 用户截图里的功能在代码中尚不存在，是新组件——"闪念视图"设置弹层
- 分析：HEO 风格的 310px 圆角 2xl 弹层，6 节：隐藏桌面编辑器 / 内容宽度 / 卡片间距 / 单栏样式 / 卡片列数 / 排序方式
- 新建文件：app/src/components/Common/PopoverFloat/blinkoHomeSettingsPop.tsx
- 新增 3 个 config 字段（shared/lib/types.ts）：cardSpacing / noteListStyle / noteListSortBy
- 卡片列数复用已有 large/medium/smallDeviceCardColumns（同步更新三档，小屏封顶 2）
- 服务端排序改造：note.ts 优先用 noteListSortBy，回退 isOrderByCreateTime，再回退 updatedAt
- 主页 pages/index.tsx：Masonry 用 CSS 变量 --blinko-card-spacing 注入卡片间距；noteListStyle=byDay/byWeek 时改用 timeline 分组渲染（复用 todo 时间线视觉）
- Layout 头部接入新组件（FilterPop 左侧）
- i18n：zh/en/zh-TW 三语补齐 13 个新词条

## 闪念视图设置按钮位置调整（2026-09-24 00:29）
- 反馈链：先放头部右侧按钮组 → 用户截图让挪位置 → 误判为侧栏 → 用户澄清为"输入框上面的闪念"（即头部左侧标题栏的"闪念"二字右边）
- 最终位置：Layout/index.tsx 标题栏 .font-black '闪念' 文字右侧，仅在 currentTitle === 'blinko' 且 isPc 时显示
- 从头部右侧按钮组（搜索→...→通知）移除，移到标题旁边，与刷新/删除按钮同一行
- Sidebar 中错误添加的 import 已清理

## 🚨 站点白屏故障定位与修复（2026-09-24 00:45）

**现象**：用户报告"站点无法正常打开"（白屏）。

**根因**（`Uncaught TypeError: t2.isoWeek is not a function`）：
- 上一轮实现的「闪念视图设置」中，`noteListStyle` 支持 `byWeek` 模式
- `pages/index.tsx` 的 byWeek 分支用了 dayjs 格式符 `WW`（`YYYY-[W]WW`）
- `WW` **必须依赖 `dayjs/plugin/isoWeek`**，但 `app/src/lib/dayjs.ts` 只注册了 utc/timezone/relativeTime
- 用户在弹层点了「按周」→ 值持久化到 DB（`config.noteListStyle = byWeek`）→ **每次打开主页都执行该分支 → 抛错 → React 整树崩溃白屏**

**修复**：`app/src/lib/dayjs.ts` 新增 `import isoWeek from 'dayjs/plugin/isoWeek'` + `dayjs.extend(isoWeek)`。
全项目仅 `pages/index.tsx:202` 一处使用 `WW`，已确认无同类隐患。

**验证**：注入 token 后实测主页正常渲染（root 73146 字符、路径停留 `/`、无 vite-error-overlay、时间线"今天"分组正常），截图确认侧栏/编辑器/卡片均正常。

**排查方法论（重要，可复用）**：
1. `curl` 各模块状态码全 200 + 登录页(/signin)能正常渲染 → 排除编译错误与模块图问题，锁定「登录后渲染期」错误
2. 登录页正常 ⇒ 静态 import 的模块（含 Layout→新组件）都能求值 ⇒ 崩溃在**组件渲染阶段**
3. **无密码复用登录态**：`SELECT "apiToken" FROM accounts WHERE id=1` 取 JWT，再
   `localStorage.setItem('blinkoToken', JSON.stringify({token:'<JWT>', user:{id:'1',name:'admin'}}))`
   → 刷新即进入登录态，无需知道密码（前端 key = localStorage.blinkoToken，路由守卫要求 tokenData.user.id 存在）
4. `agent-browser eval` 检查 `location.pathname` / `root.innerHTML.length` / `vite-error-overlay`

## 提取 app.blinko.space 的图标体系（2026-09-24 01:05）

**背景**：用户指出现有代码来自站点 https://app.blinko.space/，要求获取站点实际使用的核心图标。

**结论（重要）**：站点**已换用 Iconly**，与本地仓库的 Iconify 方案是两套体系。
- 统一入口：单个 `Icon` 组件，把任意 `prefix:name`（含 Iconify）归一化为 Iconly 图标
- 三层映射：导航 7（blinko/notes/ai/analytics/resources/marketplace/trash）+ 语义 96 + 品牌 25（brand-*）
- 归一化函数 `m()`：关键词降级（chevron-*→方向、delete/trash→trash、spark/magic/brain→ai、hash→hashtag…），兜底 `info`
- 双轨资源：
  - 静态 `assets/iconly/flat/*.svg` = 132 个（懒加载独立分片）
  - 内联进 `IconlyFlatIcon` 分片的 22 个基础 SVG（Iconly 原始 Title 命名：Add User / Paper Plus / Shield Done…）
  - 动效 Lottie `assets/iconly/{actions,ai,waiting}/*.json` = 76 + 4 + 3 = 83 个，运行时 `lottie-web`
  - AI 专轨 `ai-motion:` 前缀 → 13 条映射到 ai/* 与 actions/*
- 站点版本号：Lottie v5.8~5.12.1 / 512×512 @60fps

**提取方法（可复用）**：
1. 从 HTML 取入口 bundle → 站点是 Vite SPA，应用代码懒加载；用 `agent-browser eval` 读
   `performance.getEntriesByType('resource')` 拿到首屏已加载的全部分片名
2. 批量下载分片，定位图标调度 chunk（本次是 `icons-*.js`，10KB）
3. 用括号匹配解析压缩 JS 里的对象字面量，抽取 `导航/语义/品牌/精确覆盖` 四张映射表
4. 从 `IconlyFlatIcon-*.js` 提取 `路径→chunk` 懒加载映射（83 条 lottie），批量下载
5. 分片是 Rollup ESM：变量定义 + `export{...as default}` → 加 `package.json {"type":"module"}`
   后用 Node 动态 `import()` 取 `mod.default`，即得标准 Lottie JSON（`JSON.parse()` 形式的分片同样处理）
6. 生成自包含总览 HTML（内嵌 83 个 lottie，用 `IntersectionObserver` 懒创建实例 + hover 播放）

**注意坑**：
- `assets/iconly/flat/*.svg` 直链 **404**（资源被内联进 JS，无独立文件）
- flat 的 132 个清单（kebab-case）与内联 22 个（Title Case）**命名体系不同**，不可混当同一套
- 部分下载会 `IncompleteRead`/超时，需重试 2~3 次
- Bash 工具的 PATH 偶发丢失（`command not found: curl/dirname`）→ 改用 Python `urllib` 或绝对路径

**交付物**：`/Users/baihe/Code/blinko/.workbuddy/outputs/blinko-icons/`
`index.html`(2.2MB 总览) + `iconly-lottie/`(83 JSON) + `iconly-flat/`(22 SVG) + `manifest.json` + `README.md`

## 引入 HarmonyOS Sans 字体（2026-09-24 01:15）

**两套独立机制（易混淆，务必区分）**：
1. **默认 UI 字体**：`globals.css` 的 `@font-face` + `--font-family`（含 `:lang(zh-CN)`/`:lang(zh-TW)` 分支），
   由 `lib/harmonyFont.ts` 懒加载 CJK 大字体（SC 8MB / TC 4MB），入口挂在 `lib/i18n.ts` 的 `bindHarmonyFontToI18n(i18n)`。
   → 全站默认就是 HarmonyOS Sans，但**不出现在字体下拉里**。
2. **字体切换下拉（FontSwitcher）**：数据来自 DB `fonts` 表（`api.fonts.list.query()`），
   由 `prisma/defaultFonts.ts` 的 `cdnFonts` + `scanLocalFonts`（只扫 `.woff2`）写入。
   → 要让某个字体出现在下拉，必须往 `fonts` 表加一条记录。

**本次改动**（让 HarmonyOS Sans 出现在下拉）：
- `prisma/defaultFonts.ts`：`cdnFonts` 中「Chinese Fonts」分组前新增一条 `HarmonyOS Sans`
  （`name:'HarmonyOS Sans'`, `url:null`, `isLocal:false`, `sortOrder:115`, weights `[400,500,600,700]`）。
  因 `@font-face` 已在 globals.css 声明，`FontManager.loadFont` 走 `url:null` 分支无需额外文件，
  `applyFont` 设置 `--font-family:"HarmonyOS Sans"` 后浏览器直接命中已有 face。
- 手动 upsert 到 DB：`fonts` 表 `updatedAt` 字段**无非空默认值**（Prisma `@updatedAt` 需应用层维护），
  直接 INSERT 会报 `null value in column "updatedAt"`，必须显式带 `createdAt/updatedAt=now()`。

**字体文件**：`app/public/fonts/HarmonyOSSans/`（`HarmonyOS_Sans_Regular.ttf` 146KB 西文 + `_SC_` 8MB 简中 + `_TC_` 4MB 繁中）。

**启动命令**：`bun run dev:backend`（单端口 1111）；bun 已装在 `~/.bun/bin/bun` 但未入 PATH，
  首次需 `echo 'export PATH="$HOME/.bun/bin:$PATH"' >> ~/.zshrc && source ~/.zshrc`。

## 闪念视图设置：内容宽度四档 + 样式对齐参考图（2026-09-24 01:40）

**用户需求**：内容宽度四档 = **860px / 1120px / 1440px / 全宽**（原实现是 1200/1600/2100/2800 标 xs/sm/md/lg）。

**改动**（`blinkoHomeSettingsPop.tsx`）：
- `WIDTH_OPTIONS` → `[{860,'860px',glyph:12},{1120,'1120px',18},{1440,'1440px',24},{0,'full-width',30}]`；
  **全宽存 0**，与消费端 `pages/index.tsx:134` 的 `maxWidth: v ? v+'px' : '100%'` 天然契合
- `LEGACY_WIDTH_MAP`：旧值 1200→860 / 1600→1120 / 2100→1440 / 2800→0（显示归一，点选即写新值）
- 内容宽度分段改**图形化**：`WidthGlyph`（宽度递增圆角矩形，`bg-current` 随选中态变色），`iconOnly` 模式下 label 仅作 aria-label/title
- 配色对齐参考截图（从截图取色）：滑杆黄 **#fbe573**（filler+thumb）、分段选中块 `bg-default-300/80 dark:bg-default-600`、
  section label 改 `text-sm text-default-400`（去 uppercase/tracking-wider）、右侧数值 `text-sm font-medium text-foreground`
- i18n：三语补 `full-width`（全宽/Full width/全寬）；顺手修 zh-TW `created-at` 误翻「在」→「建立時間」

**验证**（Playwright + 本机 Chrome channel，token 注入）：
- 弹层正常渲染，四档图形显示，点击「全宽」→ `[REQ] value:0` → `[RES] 200` → DB `{"value":0}` → 前端 `max-width: 100%` ✓
- 已把 DB 恢复为 1120 档（参考图默认选中第二档）
- ⚠️ 第一次自动化点击后 DB 未变是**时序问题**（点击后等待太短就 close browser），不是代码缺陷；抓包重测即确认

**坑**：Bash 工具 grep 偶发返回空（PATH 异常）→ 用 `/usr/bin/grep` 绝对路径；Edit 拼接导致 `type WidthKey` 重复定义一次，已清理

## 黄色 #fbe573 加入主题色可选色板（2026-09-24 01:50）

- 用户需求：把滑杆黄 `#fbe573` 做成主题色、支持配置内选择 → 用户澄清「加入可选色板」即可
- 改动：`app/src/components/Common/Theme/ThemeColor.tsx` 的 `colors` 数组顶部新增
  `{ name:'yellow', background:'#fbe573', foreground:'#1c1c1e' }`（浅黄底 → 深色前景保证对比度）
- 链路（已存在，无需改）：ThemeColor 色板 → `api.config.update` 写 `themeColor`+`themeForegroundColor` →
  `store/user.ts` 把两值设为 `.dark/.light` 根元素的 `--primary`/`--primary-foreground` CSS 变量
- 当前 DB 主题色 = `#ea580c`（橙）+ foreground `#ffffff`（白），未改动（用户只要求加可选，不改默认）
- 注意：`user.ts` 判断 `themeColor && themeForegroundColor` 同时存在才应用；选黄色时 foreground 会一起写成深色，链路正确

## 首页右侧渐变光晕问题（2026-09-24 01:55）

- **现象**：用户切到黄色主题后，首页右侧右上角有一团淡紫红渐变光晕，与本意「右侧和左侧一样单色」不符
- **根因**：`Layout/index.tsx` 内容区右上角硬编码的装饰光晕 div（`bg-[#9936e6]` + `blur-3xl` + `opacity-20` + clipPath 圆形，350×350，`top-[-37%] right-[5%]`）——紫红色与黄色主题冲突
  - 注：Sidebar 左侧也有一个 halation 光晕（`#ffc65c` 黄 + circle 35%），与黄主题同系所以视觉上不突兀
- **修复**：删除 Layout 右侧光晕 div；文件里 116-118 行还有一处同样的注释掉的死代码（`{/* ... */}`），不渲染、保留
- **验证**：截图确认右侧恢复纯色单色，与左侧一致
- 注意区分三个「背景渐变」来源：① GradientBackground（ShaderGradient 水波，仅 signin/signup/share/hub/设置预览用）② Layout 内容区光晕 div（本次删除）③ Sidebar halation（保留）

## 滚动条改中性灰 + 贴右缘（2026-09-24 01:50）
- 用户：黄色滚动条（跟随主题色 --primary）太扎眼，要改成中性色、并贴到窗口右侧边（原先被 maxHomePageWidth=1120 的限宽容器内缩）
- 改动：`globals.css` `::-webkit-scrollbar-thumb` 从 `var(--primary)` 改 `var(--scrollbar)`（亮色 #d6d7d9 / 暗色 #3f3f46），
  hover 用 `color-mix(var(--scrollbar) 75%, var(--foreground))`。`--scrollbar` token 之前定义了但从未被用。
- 限宽层内移：`pages/index.tsx` 把 maxHomePageWidth 从最外层 div 移到 ScrollArea 内部的 `<div class="px-2 md:px-6 mx-auto w-full" style={maxWidthStyle}>`，
  使滚动容器保持全宽（滚动条贴窗口右缘），内容仍居中限宽。
- ⚠️ 并行 Edit 竞态坑：两次改 index.tsx 时开/闭标签错位、改 globals.css 时 thumb 规则被覆盖回旧值 → 必须逐个 Edit 并 grep 复核落盘结果

## 🚨 localhost 登录报"密码错误"根因（2026-09-24 01:52）
- **现象**：`localhost:1111/signin` 报密码错误，`0.0.0.0:1111/signin` 正常
- **根因（非密码错、非代码逻辑错）**：macOS 上 `localhost` 优先解析到 IPv6 `::1`，而服务
  `app.listen(1111, "0.0.0.0")` 只监听 IPv4，导致浏览器连 `::1:1111` 被系统代理/连接拒绝（`upstream connect failed: Connection refused` → 502），
  前端把请求失败误报为"密码错误"。`0.0.0.0` 是纯 IPv4 地址，直连成功。
- **修复**：`server/index.ts` 两处 `app.listen(PORT, "0.0.0.0")` 改 `"::"`（双栈，同时监听 IPv4+IPv6），
  `lsof` 确认从 `IPv4 *:1111` 变为 `IPv6 *:1111`，`[::1]:1111` 从 502 → 200。bun --watch 自动重启生效。
- **验证方法（可复用）**：`curl -o /dev/null -w "%{http_code}" http://[::1]:1111/signin` 对比 `http://127.0.0.1:1111/signin`；
  `lsof -nP -iTCP:1111 -sTCP:LISTEN` 看监听的是 IPv4 还是 IPv6。

## admin 密码重置为 123456（2026-09-24 01:52）
- 用户要求重置密码。账号唯一（admin / nickname Brian / superadmin），密码哈希格式 `pbkdf2:<salt>:<hash>`（PBKDF2-SHA512, 1000 次, 64 字节）。
- 用 Node `crypto.pbkdf2` 生成新哈希并 `update accounts set password=...`，密码改为 **123456**。
- 已通过真实登录接口验证：`POST /api/auth/login {"username":"admin","password":"123456"}` → 200 + token；错误密码仍 401。
- 注意：密码哈希生成必须复刻 `prisma/seed.ts` 的 `hashPassword`（randomBytes(16) salt + pbkdf2 1000/sha512/64），不能套用 bcrypt。

## 偏好设置分组重构 + 全局排版（2026-09-24 02:00）

**用户需求**：对齐官方站点设置的分组交互样式，并在偏好设置内完善「全局排版」配置能力。

**PerferSetting.tsx 全量重构**：
- 原先 20+ 个 Item 平铺 → 五个 `SettingSection` 分组（组标题+副标题+白色圆角卡 `bg-content1 heo-shadow-card`+行间 `divide-y`）：
  ①外观与语言（主题/主题颜色/语言/默认首页/背景动画）②全局排版（新增）③导航与阅读（移动导航/隐藏通知/隐藏评论/排序/首页宽度/折叠阈值/每日回顾/设备卡列）④内容显示（每页条数/时间格式/工具栏/Hub）⑤高级（管理员：自定义标题/背景/登录页脚）
- 新增 `ItemLabel`（标题+灰色描述两行）；原 ItemWithTooltip 提示改为行内描述
- **全量 Write 覆盖**前必须先 Read（本项目规则），且此文件行多，拆 Edit 易错 → Write 一次成型

**全局排版新能力**：
- config 新增 `fontSize`(12-20, 默认16) / `fontWeight`(300-700 step100, 默认400)——`shared/lib/types.ts` 的 ZUserPerferConfigKey + ZConfigSchema 两处都要加
- 应用链路：`user.ts` config 应用处把两值 setProperty 到 `.dark/.light` 根元素 → CSS 变量 `--app-font-size/--app-font-weight`
- 消费端：`globals.css` body + `github-markdown.css` `.markdown-body`（笔记卡片正文，移动端 `calc(size - 1px)`）
- UI：字号/字重 HeroUI Slider（黄滑杆），**onChange 即时 setProperty 预览（不写库），onChangeEnd 才 saveTypography 写库**（HeroUI Slider 支持 onChangeEnd，`data-slot="thumb"` 选择器可自动化）
- 预览框：`记录每一个闪念 · Typography preview`，inline style 跟随本地 state
- i18n 三语补 21 词条（分组标题/副标题/各行描述）

**实测**：分组渲染✓、CSS 变量生效✓、键盘操作滑杆 → label 14px + `--app-font-size:14px` + DB `fontSize=14` 全链路✓，已恢复 16。

## 视图设置 Popover 闪烁修复（02:10）
- 点击闪念视图设置按钮屏幕闪一下：`blinkoHomeSettingsPop.tsx` 的 Popover 用了 `backdrop="opaque"`（全屏不透明遮罩）→ 改 `backdrop="transparent"`
- 记忆点：HeroUI/NextUI Popover/Modal 的 backdrop 默认即 opaque，弹层组件"闪一下"先查 backdrop

## 顶部刷新按钮移除（02:15）
- Layout/index.tsx header 移除 fluent:arrow-sync-12-filled 刷新图标（trash 分支红色清空回收站按钮保留）

## 卡片/侧栏视觉四连改（02:17）
1. 侧边栏整体字号 14px：`Layout/Sidebar.tsx` 根 div style 加 fontSize:'14px'（导航+标签列表级联生效）
2. 去掉正文标题「闪念」前主题色竖杠：Layout/index.tsx 删 `w-[4px] h-[16px] bg-primary` div
3. 卡片底部类型标签（⚡闪念/待办/笔记）移到右下角：`BlinkoCard/cardFooter.tsx` CardFooter 重排为 [TagChips][ml-auto: ConvertTypeButton + RightContent]
4. 正文 #标签 提取到卡片左下角单独展示：
   - noteContent.tsx 新增导出 `getNoteTagPaths`（buildHashTagTreeFromDb + generateTagPaths + 仅留最深路径）与 `stripTagTokens`（仅剔除与关联标签匹配的 token；纯标签行整行删，防空段落；`/^#+$/` 排除 markdown 标题符；支持尾部 。？* 变体）
   - NoteContent 的 MarkdownRender content 改用 stripTagTokens 后的展示内容（编辑器/详情页不受影响）
   - cardFooter.tsx 新增 `CardTagChips`（blinko-tag 样式、点击跳 `/?path=all&searchText=#path`、stopPropagation、shareMode 不可点）
- 验证：4 模块 Vite 转译 200；agent-browser 注入 token 实测——竖杠消失、侧栏 14px、#开始使用/#Blinko 从正文移至左下角、类型标签在右下角

## 笔记页标签筛选 chips（参考"我的灵感"交互）（02:27）
- 需求：笔记列表顶部加一排标签筛选胶囊（全部 N / 标签 N），选中实心主色、未选中白底描边，样式对齐站点黄主题
- 服务端：`server/routerTrpc/tag.ts` 新增 `listWithCount`（tags+noteCount 排除回收/归档，附 blinko/note/todo 三类 count）
- store：`blinkoStore.tagList` 改调 listWithCount，返回新增 `tagCounts`/`viewCounts`（falttenTags/listTags/pathTags 保持不变，旧使用方无感）
- 新组件：`components/Common/TagFilterChips.tsx`——吸顶 sticky top-0、横向滚动 hide-scrollbar、emoji/iconify 双态图标、count>0 过滤、完整路径名（父/子）
- 集成：`pages/index.tsx` 限宽层首行 <TagFilterChips/>；archived/trash 视图隐藏；「全部」数字随视图切换（blinko/note/todo/all 总数）
- ⚠️ 关键坑：点标签只 navigate 会失效——blinkoStore.useQuery 里 noteList.resetAndCall 在 if(tagId) 赋值**之前**执行，首次查询不带 tagId。必须像 TagListPanel 一样先 `blinko.updateTagFilter(id)` 再 navigate（useQuery 开头有 `Number(tagId)===config.tagId → return` 防重入守卫）
- 验证：curl tRPC 端点 200；浏览器实测——数据分析筛出 1 条、开始使用 2 条、全部回切 8 条，侧栏选中态联动

## 输入框功能对比 + 编辑器选型调研（02:50）
- 反编译 app.blinko.space：线上新版是 **Slate 块编辑器**（非本地 Vditor markdown），占位「写下你的想法…」
- 线上输入框能力清单（i18n 2319 条提取）：区块类型选择器（标题1-6/列表/编号/选择/任务/引用/折叠/代码块/行内代码/表格/公式/源码模式/Sandpack）+ 斜杠菜单 + #标签/type-to-create-tag + AI写作/扩写/润色/翻译/自动打标签/AI Emoji/选中提问
- 本地仅有：Vditor markdown + 底部工具栏（NoteType/Hashtag/Reference/ResourceReference/AIWrite/Upload + ViewMode/Send）
- 选型结论（用户问类 Notion 编辑器）：推荐 Tiptap（Markdown 扩展平滑承接旧数据 + SlashCommand/DraggableBlocks/Table 官方扩展）；开箱即用首选 BlockNote；Novel 适合 Notion AI 体验

## Tiptap 类 Notion 编辑器 Demo（03:00）
- 位置：`.workbuddy/outputs/tiptap-demo/index.html`，esm.sh CDN + importmap 单文件方案（零安装、不污染主项目依赖），python http.server 8899 提供服务（agent-browser 需用 127.0.0.1，localhost 会因 IPv6 拒连）
- 功能全验证：斜杠菜单（触发/过滤/↑↓/Enter 执行）、markdown 快捷输入（## + 空格转 H2）、气泡菜单加粗、任务清单、块拖拽（手柄按顶层块定位，PM view.dragging={slice,move:true} 协议）、getHTML 实时输出
- 踩坑记录：
  1. esm.sh 双 React 实例 → 所有 @tiptap 包必须加 `?external=react,react-dom`，react-dom 也加 `?external=react`，importmap 统一 react@19.2.0（tiptap 2.11 需 React 19）
  2. `@tiptap/suggestion` 导出的是 PM 插件工厂函数，需 `Extension.create({addProseMirrorPlugins(){return [Suggestion({editor:this.editor,...})]}})` 包装，不是 .configure
  3. Suggestion 必须配顶层 `command` 选项（默认 ()=>null），否则 Enter 选中无动作；链路：SlashList command(item) → props.command → 顶层 command({editor,range,props})
  4. BubbleMenu+自定义手柄同 React state 会渲染风暴崩溃 → 手柄直接 DOM 定位（display/top 直改，不走 setState）
  5. 气泡按钮必须 onMouseDown preventDefault，否则选区丢失加粗失效
  6. TaskItem 初始 HTML 需 `li[data-type="taskItem"]`（parseHTML 规则要求），渲染后 data-type 不出现但 data-checked 在
  7. Suggestion 默认 allowedPrefixes:[' '] 要求斜杠前是空格，段首/空格后可触发；测试用 execCommand('insertText') 不触发（走 mutation 非 handleTextInput），需 agent-browser keyboard type 真实按键
- 选型结论不变：正式迁移用 Tiptap npm 包（React 18 兼容需用 @tiptap/react@2.x 对应版本），demo 已验证交互模式可行

## Tiptap 接入预览页（03:18，供用户决策）
- 文件：`.workbuddy/outputs/tiptap-demo/blinko-preview.html`（127.0.0.1:8899/blinko-preview.html）
- 复刻 Blinko 编辑器外层（卡片边框 focus-within 变主色、附件条、底部工具栏：类型/标签/引用/资源/AI/上传|视图|发送），内核换 Tiptap
- 预览图：blinko-preview.png（默认态）、blinko-preview-slash.png（斜杠菜单态）
- 待用户确认后再动正式代码；迁移要点：markdown 双向转换、KaTeX/mermaid/代码高亮扩展接入、editorStore 对接 store.noteContent

## Tiptap 正式接入完成 + 全功能实测通过（04:00）
用户确认底部工具栏方案后正式切换。改动清单：
- 新建 `app/src/components/Common/Editor/Tiptap/`：adapter.ts（冒充 vditor 接口）、extensions.ts（SlashCommand 10 命令）、ToolbarButtons.tsx、DragHandle.tsx、TiptapEditorContent.tsx、slashMenuState.ts、tiptap.css
- `hooks/useEditor.ts`、`editorStore.tsx`、`editorUtils.tsx` 去 Vditor 化；`index.tsx` 新底部工具栏（⚡类型 / #标签 / 🖼图片 | Aa / 无序 / 有序 | @引用 + 发送）
- 依赖：@tiptap/react|pm|starter-kit|suggestion + extension-{placeholder,task-list,task-item,highlight,underline,link,image,table,table-row,table-header,table-cell}@2 + tiptap-markdown@0.8
- i18n 三语去重并重插 16 个格式词条（旧"订单列表/下划线"错译已修）

**实测通过清单**（agent-browser，token 注入）：
- Aa 菜单：展开、标题1（h1）、正文（h1→p 回切）、加粗激活态高亮指示 ✓
- 气泡菜单：选区出现（B/I/U/S/高亮/行内代码）、加粗 → `<strong>` ✓
- 列表按钮：p→ul、ul→ol 直接切换 ✓
- # 标签：Popover 搜索+列表、光标处插入 `#测试标签2 `（&nbsp;→空格）、原内容保留 ✓
- @ 引用：Popover 笔记列表、添加引用卡片（黄色高亮条）✓
- 发送：落库 markdown（id=34 `今日灵感记录#测试标签2 `）、发送后编辑器重置+成功 toast ✓
- 斜杠菜单 10 命令、任务清单快捷输入、存量笔记 round-trip（前次已验）✓
- 图片上传未实测真实文件（input file 存在，走原 store.uploadFiles 链路与编辑器无关）

**自动化测试技巧**（复用）：
- React Aria Popover 对 `.click()` 无响应 → eval 派发 pointerdown/pointerup/mousedown/mouseup/click 五事件序列（带 clientX/Y）
- 普通 onClick 的 div（列表按钮/发送按钮）用 path d 前缀定位 + `.click()` 即可
- Tiptap 行为注意：失焦后 focus() 会恢复上次选区——全选状态下通过 Popover 插入内容会替换整个选区（编辑器惯例语义，非 bug）

## 任务清单按钮 + Notion 高亮块（Callout）（04:15）

**需求**：①任务清单按钮加入底部工具栏 ②类 Notion 高亮块

**改动**：
1. `Tiptap/Callout.ts`（新建）：自定义 Node——`content:'block+'`、attr `type`(info/warning/success/danger)、parseDOM `div[data-type="callout"]`
2. **tiptap-markdown 自定义序列化关键**：节点 `addStorage()` 里给 `storage.markdown = { serialize(state,node){ state.write(getHTMLFromFragment(Fragment.from(node),schema)); state.closeBlock(node) } }`
   → 存库为 HTML block `<div data-callout-type="warning" data-type="callout"><p>..</p></div>`；读取时 markdown-it(html:true) 保留 HTML、parseDOM 还原，round-trip 无损
   ⚠️ 不能用 `state.renderContent(node)`（子块会走 markdown 序列化，裸露在 HTML block 里不被解析）
   ⚠️ 需用 `getHTMLFromFragment` 整体输出 HTML（镜像 tiptap-markdown HTMLNode 策略）
3. **命令用 wrapIn 而非 setNode/toggleNode**：`setNode('callout')` 对段落光标返回 false（block 容器不适用）；正确 toggle：`editor.isActive('callout') ? chain().lift('callout') : chain().wrapIn('callout', attrs)`
4. `ToolbarButtons.tsx`：+`TaskListButton`（mdi:format-list-checks）、+`CalloutButton`（💡图标 + 4 色类型选择 Popover）
5. `index.tsx` 底部工具栏列表组加两按钮：⚡ # 🖼 | Aa 无序 有序 任务 💡 | @ 发送
6. `extensions.ts` 斜杠菜单 +callout 项（/cal 可过滤）
7. 样式双端：`tiptap.css`(.tiptap 作用域) + `github-markdown.css`(.markdown-body 作用域)——emoji 徽标 `::before` **绝对定位 left:13px**（文档流会单独占行），容器 padding-left:44px；CSS 变量 --callout-accent/bg/icon 按类型覆盖
8. i18n 三语 +5：callout/callout-info(提示)/warning(警告)/success(成功)/danger(危险)

**实测全通过**：任务按钮 p→taskList ✓；CalloutButton 四类型 toggle 包裹/解除 ✓；斜杠 /cal Enter 创建 ✓；序列化 `<div data-callout-type=...>` ✓；setValue round-trip ✓；发送落库 id=36 ✓；**查看端卡片渲染 Notion 风格**（rehypeRaw 渲染 HTML + markdown-body 样式）✓

**测试坑**：adapter.setValue 走 setContent(md,false) 不触发 onUpdate → store 不同步；但 handleSend 用 getValue() 实时取值，真实用户链路无碍（自动化测试须用 insertContent/toggle 命令构造内容再发送）

## Callout 自定义 emoji（04:26，用户问"高亮块图标能否手动选高频表情"）
- **改动**：Callout 节点新增 `icon` 属性（default null，renderHTML 回退 CALLOUT_DEFAULT_ICONS[type]）；新增 `setCalloutIcon`/`setCalloutType` 命令（updateAttributes 实现）
- **CSS 关键**：emoji 从 `var(--callout-icon)` 硬编码改为 `content: attr(data-callout-icon)`（编辑端 tiptap.css + 查看端 github-markdown.css 同步改）；type 只管颜色，icon 完全独立
- **UI**：💡 按钮 Popover 改两段式——颜色 4 选项（当前项高亮）+ 20 个高频 emoji 网格（💡📌✅⚠️🔥🚀🎯📝⭐❗⏰💪🧠🎉👀💬🔔📊🛠️📅）；i18n 加 callout-color/callout-icon
- **行为**：无 callout 时选色/选图标直接创建（带 attrs）；有 callout 时原地 update；切换颜色**图标保留**
- **实测**：⚠️默认填充→📌替换→🔥从零创建→danger 换色图标保留→round-trip（🚀 还原/无 icon 回退默认）→落库 id=38→卡片红色🔥渲染 ✓

## 随机图标按钮 + 任务清单对齐修复（04:30）
- **随机按钮**：emoji 面板标题行右侧加「随机」，从 20 个池子里排除当前图标后随机取（避免原地踏步）；无 callout 时创建、有 callout 时原地替换
- **任务清单对齐根因**：li 是 flex+flex-start，checkbox 靠 margin-top:4px 手拍对齐，与文字首行（.tiptap p 有 margin-bottom:8px 干扰）不匹配
- **修法**：label 改 display:flex+align-items:center+height:24px（匹配首行行高），li>div>p margin-bottom:0；量化验证 checkbox/text 首行垂直中心 delta=0
- **DOM 结构备忘**：@tiptap/extension-task-item 渲染 `<li><label><input><span></span></label><div><p>文字</p></div></li>`

## 筛选标签被图片盖住修复（04:34）
- **根因**：TagFilterChips 是 `sticky top-0 z-10`，而 HeroUI `Image` 组件默认 class 带 `relative z-10`（`opacity-0 shadow-black/5...`）。两者同级 z-10 竞争，图片在 DOM 中靠后 → 滚动时图片盖住标签
- **修法**：TagFilterChips 的 z-10 提到 z-20（chips(20) > img(10)），一处改动
- **验证**：构造图片卡片滚到与标签重叠，elementFromPoint 探标签中心 → 命中 chips 内 SPAN（isChips=true），图片被正确压在下面
- **备忘**：HeroUI Image 默认 class 含 `relative z-10 opacity-0`；sticky 元素要与卡片图片竞争时需高于 z-10

## Callout 交互三连修 + 任务清单删除线修复（04:38）
- **1. 光标困住**：callout 是文档最后一块时点下方空白，光标被塞回 callout 内（PM 把末尾点击定位到最后块内）。修：CalloutClickOutside 插件 handleClick——点击 Y > callout DOM bottom 且最后块是 callout → 在 doc 末尾插入空段落并定位光标
- **2. 退出快捷**：addKeyboardShortcuts——Enter 在「最后子块+空块」时 lift(this.name) 提出空块（无残留）；Backspace 在「第一子块+开头」时 lift 整个 callout 一次移除格式。Node 的 shortcut 与 StarterKit 竞争靠返回 true 阻断
- **3. 图标点击弹窗**：插件 handleDOMEvents.mousedown 检测点击是否落在 callout 左侧 gutter（left+4~38, top+2~34 图标区）→ 派发 `callout-icon-click` CustomEvent；新组件 CalloutIconMenu 监听后在鼠标位置渲染浮动面板（复用抽出的 CalloutPickerContent），Esc/外点关闭
- **坑**：`calloutEl.contains(view.dom)` 写反（应 view.dom.contains(calloutEl)）导致事件永不触发
- **4. 任务清单删除线**：根因是 ListItem 组件 `childArray.find(child => child?.type === 'input')` 只找直接子元素——loose list（条目间有空行）时 mdast-util-to-hast 把 input 嵌在 <p> 里（tight list 才展开到 li 直接子级），find 不到 → isChecked 永远 false。修：新增 findCheckbox 递归查找。tight/loose 差异也解释了为何新笔记（单条 tight）正常、旧笔记（多条 loose）不显示删除线

## 任务清单「只能生效 1 个」根因修复（05:00）
- **表象**：列表页点击任务项完成/取消完成，连续点多个只有 1 个生效
- **双重根因**：
  1. `replaceTaskMark` 用 `content.indexOf("]任务文本")` 定位——源 markdown 里 `]` 与文本之间有空格，`"]文本"` 匹配不到 → 返回 -1 直接原样返回；重复文本永远命中第一个
  2. 连续点击时 `onChange` 用闭包里旧 content 计算，覆盖前一次结果（stale closure）
- **修法**：
  - 改用**序号定位**：remark-task-list 会给每个任务项 li 注入 `id="task-list-item-N"`（N=DFS 全局递增序号，经 data.hProperties→hast properties.id）。`toggleTasksByIndex(content, indexes, targetState)` 逐行正则 `^(\s*[-*+]\s+)\[([ xX])\]` 扫描计数，翻转第 N 个
  - **onChange 改函数式**：签名从 `(newContent)=>void` 改为 `(updater:(current)=>string)=>void`，调用方 `updater(blinkoItem.content)` 基于最新 store 值计算，消除 stale closure
- **关键坑**：react-markdown 的 `components.li` **只对顶层 li 生效**，嵌套子任务 li 是默认渲染的 `<li>`（props 有 `node`/`id`/`className`，无自定义 taskIndex prop）。所以 `getChildTasks` 从子 li 的 `props.id`（而非 taskIndex）解析序号
- **调用方**（3 处 onChange）：noteContent.tsx / review.tsx / FullscreenEditor.tsx，均已改为 updater 形式
- **实测**：4 个任务项连续异步交替点击（完成↔取消）全部独立正确；嵌套父任务点击联动所有子任务；重复文本「任务×3」点中间只命中中间

## 正文/卡片标签点击跳离当前视图修复（05:15）
- **表象**：闪念/笔记内点击 `#标签`，会 navigate 到 `/?path=all&searchText=...`，离开当前所在视图（notes→all、闪念→all）
- **根因**：三处标签点击硬编码 `path=all`：
  1. `MarkdownRender/index.tsx` 的 `HighlightTags`（正文内标签）
  2. `BlinkoCard/cardFooter.tsx` 的 `CardTagChips`（卡片底部标签）
  3. `BlinkoCard/cardBlogBox.tsx`（blog 卡片标签）
- **修法**：点击时读取 `new URLSearchParams(location.search).get('path')`，有 path 则 `/?path=<current>&searchText=...`，无 path（闪念视图）则 `/?searchText=...`；保留 forceQuery++
- **保留不动**：`GlobalSearch.tsx` 的 `navigateToTag`（全局搜索框主动搜标签，跳 all 合理）、`TagFilterChips`/`TagListPanel`（顶部 chips/侧边栏用 tagId 筛选，属主动筛选入口）
- **实测**：notes 视图点 `#数据分析/埋点体系` → URL 保持 `path=notes`；闪念视图点 `#开始使用` → URL `/?searchText=...` 无 path，均正确保持视图 + searchText 过滤生效

## 标签点击跳离当前视图修复（真正的根因，05:30）
- **上次修错了对象**：上次改的是「正文内标签」(HighlightTags/CardTagChips/CardBlogBox，走 searchText 文本搜索)，但用户实际反馈的是「顶部标签 chips / 侧边栏标签树」的标签点击，走的是 **tagId 数字筛选**，硬编码 `navigate('/?path=all&tagId=...')`
- **真正根因**（三处硬编码 all）：
  1. `TagFilterChips.goTag` → `navigate('/?path=all&tagId='+id)`
  2. `TagListPanel` 侧边栏叶子标签 → `navigate('/?path=all&tagId='+id)`
  3. `blinkoStore.updateTagFilter` → `type=-1; noteList.resetAndCall()`（强制 all 列表）
- **修法**：
  - `updateTagFilter` 改为按当前 `noteListFilterConfig.type` 选择对应 list（NOTE→noteOnlyList / TODO→todoList / -1→noteList / 其他→blinkoList），不再强制 all
  - `goTag` / `TagListPanel` 点击时读 `searchParams.get('path')`，有则 `/?path=<current>&tagId=...`，无则 `/?tagId=...`
  - 高亮判断 `isSelected`/`activeTagId` 去掉 `path=='all'` 限制，改为只看 `tagId` 匹配
- **后端确认**：`server/routerTrpc/note.ts` list 里 `type`(第153-155) 和 `tagId`(第169-172) 是独立可组合的 where 条件，notes 视图 + tagId 筛选后端完全支持
- **实测**：notes 视图点 Welcome → `/?path=notes&tagId=1`；闪念视图点标签 → `/?tagId=27`；侧边栏叶子标签 → `/?path=notes&tagId=27`；all 视图点标签仍 `/?path=all&tagId=9`，点「全部」正确清除 tagId

## 标签筛选视图约束逻辑细化（05:40）
- **需求**：区分两种标签点击来源——
  1. **侧边栏「所有标签」面板**（TagListPanel）点标签 → 跨类型（type=-1，跳 path=all）
  2. **顶部 chips**（TagFilterChips，在闪念/笔记/待办视图内）点标签 → 限制当前视图 type
- **特殊**：默认首页（defaultHomePage=blinko）地址是 `/` 无 path，等价闪念视图（useQuery 里 path 为空走 else→blinkoList，type=BLINKO）
- **改动**：
  - 后端 `tag.ts` listWithCount 每个标签新增 `blinkoNoteCount/noteNoteCount/todoNoteCount`（按 note.type 分组计数）
  - `blinkoStore.tagList` 存 `tagBlinkoCounts/tagNoteCounts/tagTodoCounts` 三张计数表
  - `updateTagFilter(tagId, type?)` 增加可选 type 参数：侧边栏传 -1（跨类型），chips 不传（保持当前 type）
  - `TagFilterChips.tagItems` 计数按当前 path 选对应计数表（notes→noteNoteCounts / todo→todoNoteCounts / all→tagCounts / 无path→blinkoCounts）
  - `TagListPanel` 恢复 `path=all` + `updateTagFilter(id,-1)`；`isSelected` 恢复 `path=='all'` 判断
- **实测**：notes 点 chips Welcome → `/?path=notes&tagId=1`（限笔记）；闪念(`/`)点 chips Blinko → `/?tagId=27`（限闪念）；notes 点侧边栏 Blinko → `/?path=all&tagId=27`（跨类型，显示 2 条 Blinko 闪念笔记）；计数按视图正确（notes 全部7/闪念全部11/all 全部20）
